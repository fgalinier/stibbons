/*
Ce code teste les tableaux.
On commence par définir deux tableaux.
On affiche dans la sortie standard le troisième élément de t (bla), puis on le modifie en y mettant le chiffre 32, on le réaffiche (32).
On affiche ensuite le tableau v, puis le tableau u, qui contient des tortues.
*/

a = 12

t = {18,red,"bla"}
v = { "bla" : "blou", "blue" : blue, a : 29 }

println(t[2])
t[2] = 32
println(t[2])
v[] = 48
println(v)

u = 5 new agent {
	while true fd 20
}

println(u)
