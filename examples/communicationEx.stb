agent star () {
	color=red
	while (true) {
		rt rand() % 20
		lt rand() % 20
		fd 1
	}
}

new suiveur ()


5 new agent {
	function wiggle(){
		while (true) {
			rt rand() % 20
			lt rand() % 20
			fd 1
		}
	}
	if (in_box != 0){ 
		recv msg exp
		face(exp)
		wiggle()
	}
	turtles=in_radius(5)
	if (size(turtles) != 0) {
		for i in turtles {
			if (i.color == red){
				println("Je l'ai trouvé !")
				coordonnee={i.pos_x, i.pos_y, i.pos_angle}		
				send coordonnee
			}
		}
			
	}
	wiggle()
	
}


