%x-warp true
%y-warp true

agent star () {
	color = red
	teleport(rand() % world.max-x, rand() % world.max-y, rand())

	while (true) {
		rt rand() % 20
		lt rand() % 20
		fd 1
	}
}

marylin = new star()

5 new agent {
	teleport(rand() % world.max-x, rand() % world.max-y, rand())

	while (true) {
		face(world.marylin)
	}
}
