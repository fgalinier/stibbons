agent star () {
	color = red
	teleport(rand() % world.max-x, rand() % world.max-y, rand())

	while (true) {
		rt rand() % 60
		lt rand() % 60
		fd 1
	}
}

marylin = new star()

5 new agent {
	teleport(rand() % world.max-x, rand() % world.max-y, rand())

	while (true) {
		face(marylin)
	}
}
