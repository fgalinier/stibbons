%x_warp true
%y_warp true

agent star () {
	color = red
	teleport(rand() % world.max_x, rand() % world.max_y, rand())

	while (true) {
		rt rand() % 20
		lt rand() % 20
		fd 1
	}
}

marylin = new star()
marylin = marylin[0]

5 new agent {
	teleport(rand() % world.max_x, rand() % world.max_y, rand())

	while (true) {
		face(world.marylin)
	}
}
