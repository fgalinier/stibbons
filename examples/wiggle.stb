%x_warp true
%y_warp true

agent ant () {
	teleport(rand() % world.max_x, rand() % world.max_y, rand())

	function wiggle() {
		rt rand() % 60
		lt rand() % 60
		fd 1
	}

	pd
	while (true) {
		wiggle()
	}
}

5 new ant()

